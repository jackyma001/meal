version: '3.4'
networks:
  dockerapi-dev:
    driver: bridge 
services: 
  frontend:
    image: mealfrontend
    restart: unless-stopped
    build: 
      context: frontend
      dockerfile: Dockerfile
      args:
          VUE_APP_API_URL: https://localhost:7242
    ports: 
      - "8080:80"
    depends_on: 
      - backend
    networks:
      - dockerapi-dev  
  backend:
    image: mealbackend
    restart: unless-stopped
    build: 
      context: backend
      dockerfile: Dockerfile
    ports: 
      - "7242:80"
    depends_on: 
      - db
    networks:
      - dockerapi-dev  
  db:
    image: postgres:latest
    ports:
      - "5432:5432"
    restart: always
    volumes:
      - db_volume:/var/lib/postgresql/data/
      - ./dbscripts/seed.sql:/docker-entrypoint-initdb.d/seed.sql
    environment:
      POSTGRES_USER: "postgres"
      POSTGRES_PASSWORD: "eeeqxxtg998"
      POSTGRES_DB: "meal"
    networks:
      - dockerapi-dev
volumes:
  db_volume:
  dbscripts: